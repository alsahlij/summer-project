/**
 * Represents the different types of pieces in the Snow Problem game.
 * Each piece behaves differently according to the game rules.
 */
public enum PieceType {
    TREE,
    SNOWBALL_LARGE,
    SNOWBALL_SMALL,
    HEAD
}